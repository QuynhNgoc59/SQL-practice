LINK: https://www.hackerrank.com/challenges/the-company/problem?isFullScreen=true
TYPE: Medium

==> My approach:
--Return a company list with their corresponding information
SELECT c.company_code
     , c.founder
     , COUNT (DISTINCT e.lead_manager_code) AS lead_manager_count
     , COUNT (DISTINCT e.senior_manager_code) AS senior_manager_count
     , COUNT (DISTINCT e.manager_code) AS manager_count
     , COUNT (DISTINCT e.employee_code) AS employee_count
FROM Company AS c  
JOIN Employee AS e   
    ON c.company_code = e.company_code
GROUP BY c.company_code
       , c.founder
ORDER BY c.company_code ;

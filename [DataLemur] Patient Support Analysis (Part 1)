LINK: https://datalemur.com/questions/frequent-callers

==> My approach:
WITH case_cal AS (
SELECT policy_holder_id
     , COUNT(DISTINCT case_id) AS case_per_holder
FROM callers
GROUP BY policy_holder_id
HAVING COUNT(*) >= 3
)
SELECT COUNT(policy_holder_id) AS policy_holder_count
FROM case_cal;
